# Final Controller Extraction Verification

Category: enhancement
Status: completed

## Parent

.scratch/frontend-app-controller-extraction/PRD.md

## What to build

Verify the completed controller extraction end to end. Confirm App is now a thin composer of runtime, controllers, and feature components, while user-visible behavior remains unchanged. Perform small residual cleanup only where it reduces App LOC without introducing new architecture or behavior changes.

## Acceptance criteria

- [x] App remains the public root component and top-level composer.
- [x] App no longer owns the major state/actions/effects for Documents, Templates, Workspace, auth, or profile.
- [x] Controller return values are grouped by UI surface rather than exposed as large flat bags of individual fields.
- [x] The runtime split avoids hook-order cycles and keeps forbidden Workspace access recovery owned by the Workspace controller.
- [x] Any residual page JSX extraction is limited to behavior-preserving cleanup and does not introduce React Context, routing, a global state library, CSS modularisation, backend API changes, storage-key changes, or UI redesign.
- [x] The final implementation preserves **Active page**, **Workspace selection view**, **Action toast**, **Document**, **Extraction job**, **Template**, **Selected upload Template**, and **Completed document cache** behavior.
- [x] No domain context update or ADR is added unless a new hard-to-reverse, surprising, trade-off-driven decision was introduced during implementation.
- [x] `npm run test --prefix frontend` passes.
- [x] `npm run build --prefix frontend` passes.

## Blocked by

- .scratch/frontend-app-controller-extraction/issues/02-extract-document-controller.md
- .scratch/frontend-app-controller-extraction/issues/03-extract-template-controller.md
- .scratch/frontend-app-controller-extraction/issues/04-extract-workspace-controller.md
- .scratch/frontend-app-controller-extraction/issues/05-extract-auth-profile-controller.md

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Verify the completed frontend controller extraction end to end, complete only minimal residual behavior-preserving cleanup, and confirm App is now a thin composer over runtime, controllers, and feature components.

**Current behavior:**
The frontend controller extraction PRD has completed the runtime/core, Document controller, Template controller, Workspace controller, and auth/profile controller slices. App remains the public root component and now composes extracted controllers and presentation components, but the final verification slice has not yet been completed. App may still contain residual page JSX or dead helper code that can be removed or extracted only if doing so reduces App responsibility without changing behavior or introducing new architecture.

**Desired behavior:**
The final state should preserve all user-visible frontend behavior while confirming that App no longer owns major state/actions/effects for Documents, Templates, Workspace, auth, or profile. App should remain the public root and top-level composer. Controller public APIs should stay grouped by UI surface, forbidden Workspace access recovery should remain owned by Workspace behavior, and the runtime split should avoid hook-order cycles. Any cleanup should be small, behavior-preserving, and limited to reducing residual App ownership or unused code.

**Key interfaces:**
- The root `App` component remains the public frontend bootstrap and composition boundary.
- The app runtime core owns endpoint construction, logging, generic request primitives, and Action toast helpers that do not depend on Workspace recovery.
- The workspace-aware request layer accepts the forbidden Workspace access callback supplied by Workspace behavior.
- The Document controller owns Document, Extraction job, upload modal, polling, deletion, selected Document, and Completed document cache behavior.
- The Template controller owns Template list/edit behavior, Template JSON modal behavior, Template field coordination, and Selected upload Template state used by Document upload.
- The Workspace controller owns Workspace context, Workspace selection view, Workspace resolution, accepted Workspace switching, invited Workspace entry behavior, invitations, users, Workspace API key display, and forbidden access recovery.
- The auth/profile controller owns auth form state, Better Auth actions, password feedback, profile editing, profile menu dismissal, sign-out cleanup coordination, and session refetch behavior.

**Acceptance criteria:**
- [ ] App remains the public root component and top-level composer.
- [ ] App no longer owns the major state/actions/effects for Documents, Templates, Workspace, auth, or profile.
- [ ] Controller return values remain grouped by UI surface rather than exposed as large flat bags of individual fields.
- [ ] The runtime split avoids hook-order cycles and keeps forbidden Workspace access recovery owned by the Workspace controller.
- [ ] Any residual App cleanup is behavior-preserving and does not introduce React Context, URL routing, a global state library, CSS modularisation, backend API changes, storage-key changes, or UI redesign.
- [ ] Active page, Workspace selection view, Action toast, Document, Extraction job, Template, Selected upload Template, and Completed document cache behavior are preserved.
- [ ] No domain context update or ADR is added unless implementation introduces a new hard-to-reverse, surprising, trade-off-driven decision.
- [ ] `npm run test --prefix frontend` passes.
- [ ] `npm run build --prefix frontend` passes.

**Out of scope:**
- Changing user-visible behavior, visual design, Active page semantics, Workspace selection view semantics, or Workspace access rules.
- Introducing React Context, URL routing, a global state library, CSS modularisation, backend API changes, storage-key changes, or persisted data shape changes.
- Broad terminology-only renames or controller-specific tests that only assert hook structure.
- Re-opening the already completed Document, Template, Workspace, or auth/profile extraction slices except for minimal integration cleanup required by final verification.
