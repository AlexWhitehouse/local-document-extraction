# Verify Application admin page end-to-end

Category: enhancement
Status: completed

## Parent

.scratch/application-admin-page/PRD.md

## What to build

Complete the cross-cutting verification pass for the Application admin page. This final slice should close any coverage gaps, ensure backend and frontend checks pass, and confirm the delivered feature matches the documented account-management boundaries.

## Acceptance criteria

- [x] Backend coverage verifies Better Auth admin plugin configuration, synthetic user admin fields, and preserved Account email verification and personal Workspace bootstrap behavior.
- [x] Frontend coverage verifies admin visibility, no non-admin layout shift, non-admin fallback, Admin page workspace-context independence, self-action guards, user-list mutations, and impersonation transition behavior.
- [x] The implementation does not expose out-of-scope capabilities: user deletion, user creation, password setting, manual session revocation, account email editing, user name editing, Workspace membership summaries, Workspace data management, custom audit logs, custom product admin routes, temporary bans, role filters, banned-status filters, or Better Auth user IDs.
- [x] The Application admin page uses Better Auth admin utilities directly and keeps admin loading/mutation state local to the admin feature.
- [x] Backend typecheck passes.
- [x] Frontend production build passes.
- [x] Any focused tests added for the feature pass.

## Blocked by

- .scratch/application-admin-page/issues/01-gate-application-admin-page-from-persisted-auth-role.md
- .scratch/application-admin-page/issues/02-list-and-search-application-admin-users.md
- .scratch/application-admin-page/issues/03-change-application-roles-with-safeguards.md
- .scratch/application-admin-page/issues/04-ban-and-unban-users-with-confirmation.md
- .scratch/application-admin-page/issues/05-start-regular-user-impersonation-safely.md
- .scratch/application-admin-page/issues/06-show-and-stop-impersonation-mode.md

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Complete the final end-to-end verification pass for the Application admin page.

**Current behavior:**
The Application admin page has been delivered across the preceding slices. It is intended to be an application-wide account-management surface for Application admins, separate from Workspace owner/admin authority. The feature now includes Better Auth admin plugin setup, Application admin visibility gating, user listing and search, application role changes, permanent ban and unban flows, regular-user impersonation, and a persistent stop-impersonating flow.

The remaining work is not to add a new capability, but to verify that the assembled feature matches the PRD and domain boundaries end to end. The prior slices are marked complete, so this issue should close coverage gaps, catch integration regressions, and confirm backend typecheck and frontend production build health.

**Desired behavior:**
The completed Application admin page should be verified as a coherent feature. Backend coverage should confirm the Better Auth admin setup and preserve existing Account email verification and personal Workspace bootstrap behavior. Frontend coverage should confirm the admin UI behaviors, workspace-context independence, local admin loading/mutation state, self-action safeguards, user-list mutations, and impersonation transitions.

The verification pass should also confirm that the first Application admin capability set does not expose out-of-scope account or Workspace administration capabilities. The page should continue to use Better Auth admin utilities directly through the existing auth client rather than custom product admin routes, and it should keep admin feature state local to the Application admin page.

**Key interfaces:**
- Better Auth configuration should include the admin plugin without config-based admin user IDs and should expose synthetic user responses that include admin plugin fields with safe defaults.
- Account email verification and personal Workspace bootstrap behavior should remain intact after adding the Better Auth admin plugin fields.
- The existing runtime auth client should expose Better Auth admin client utilities and preserve existing auth base URL and credential behavior.
- The authenticated SPA layout should derive Application admin visibility from the resolved session role before rendering the main layout, using exact single-role `admin` semantics.
- The Application admin page should own user listing, search, pagination, confirmation, mutation, loading, and error state locally.
- User-management actions should use Better Auth admin utilities directly, not custom product `/v1/admin/*` routes.
- Impersonation state should be read from Better Auth session data and the main layout should provide a persistent stop-impersonating flow.

**Acceptance criteria:**
- [ ] Backend coverage verifies Better Auth admin plugin configuration without config-based admin user IDs.
- [ ] Backend coverage verifies synthetic user responses include Better Auth admin plugin fields with regular-user defaults.
- [ ] Backend coverage verifies Account email verification behavior still requires verification for email/password access and still sends verification email as expected.
- [ ] Backend coverage verifies personal Workspace bootstrap remains after Account email verification, remains idempotent, and does not happen before verification.
- [ ] Frontend coverage verifies Application admins see the Admin sidebar item and non-admin users do not.
- [ ] Frontend coverage verifies non-admin users do not experience a content layout shift from Application admin visibility checks.
- [ ] Frontend coverage verifies stale non-admin admin-page state returns to the Workspace page rather than rendering an unauthorized Application admin page.
- [ ] Frontend coverage verifies the Application admin page remains available during Loading workspace context and Workspace resolution error.
- [ ] Frontend coverage verifies Application admin listing, manual email/name search, clearing search, newest-first pagination, fixed page size 25, total count display, inline list errors, and no Better Auth user IDs in the UI.
- [ ] Frontend coverage verifies role-change confirmations, self-demotion protection, list reload, and Action toast feedback.
- [ ] Frontend coverage verifies ban and unban confirmations, required ban reason validation, self-ban protection, explicit admin-to-admin ban confirmation, list reload, and Action toast feedback.
- [ ] Frontend coverage verifies impersonation target eligibility, start confirmation, session-scoped UI state clearing, session refetch, movement to the Workspace page, and no admin-list reload after successful impersonation.
- [ ] Frontend coverage verifies the persistent impersonation indicator, no original admin Better Auth user ID display, immediate stop-impersonating behavior, loading/disabled stop state, state clearing, session refetch, return to the Admin page, and recoverable failure feedback.
- [ ] Verification confirms the UI does not expose user deletion, user creation, password setting, manual session revocation, account email editing, user name editing, Workspace membership summaries, Workspace data management, custom audit logs, custom product admin routes, temporary bans, role filters, banned-status filters, or Better Auth user IDs.
- [ ] Backend typecheck passes.
- [ ] Frontend production build passes.
- [ ] Any focused tests added or updated for this verification pass pass.

**Out of scope:**
- Adding new Application admin capabilities beyond the first slice.
- User deletion, user creation, password setting, or manual session revocation.
- Account email editing or user name editing.
- Workspace membership summaries or Workspace data management.
- Custom product `/v1/admin/*` routes.
- Custom admin audit logging.
- Temporary ban duration, role filters, banned-status filters, page-size selection, sortable table columns, or multi-role admin UI.
- Showing Better Auth user IDs in the Application admin UI.
- Introducing URL routing, CSS modules, or a new styling system.
