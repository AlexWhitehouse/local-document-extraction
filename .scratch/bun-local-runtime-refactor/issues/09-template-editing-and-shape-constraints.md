# Template Editing And Shape Constraints

Category: enhancement
Status: completed

## Parent

.scratch/bun-local-runtime-refactor/PRD.md

## What to build

Complete local Template behavior beyond create/list: detail reads, updates, versioning, deletion/archive semantics, and Template object schema constraints should all use local **Workspace product data**. The old commercial plan limits disappear, but product-owned Template shape constraints remain enforced.

## Acceptance criteria

- [x] Template detail reads come from the local Workspace product store.
- [x] Template updates create new **Template versions** when fields change.
- [x] Template deletion/archive behavior remains compatible with existing product API expectations.
- [x] Each **Template** may contain at most one table-shaped **Template field**.
- [x] A table-shaped **Template field** counts as one top-level **Template field** and enforces the **Template object column limit**.
- [x] Removed billing plan limits do not block Template creation or updates.
- [x] Frontend Template editing, JSON modal behavior, and selected upload Template behavior continue to work.
- [x] Tests cover detail, update, versioning, deletion/archive behavior, shape constraints, and no billing limit dependency.

## Blocked by

- .scratch/bun-local-runtime-refactor/issues/04-local-workspace-product-store-template-create-list.md
- .scratch/bun-local-runtime-refactor/issues/08-retire-billing-from-product-workflows.md

## Comments

> *This was generated by AI during triage.*

Completed the local Template lifecycle: detail reads, field-versioned updates, and soft deletion now use each Workspace's Bun SQLite product store and retain the established `GET`, `PATCH`, and `DELETE 204` API contracts. Local Template validation has no commercial count or field-tier gates, while preserving one table-shaped field per Template and a fixed 20-column object-schema safety cap. The JSON modal and field editor use the same constraints, and the editor prevents a twenty-first column.

Verification passed: backend `bun run test` (396 Vitest and 41 Bun tests) plus `bun run typecheck`; frontend `bun run test` (220 tests) and `bun run build`.
