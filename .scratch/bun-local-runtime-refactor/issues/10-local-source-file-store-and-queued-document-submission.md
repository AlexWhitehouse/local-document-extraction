# Local Source File Store And Queued Document Submission

Category: enhancement
Status: completed

## Parent

.scratch/bun-local-runtime-refactor/PRD.md

## What to build

Make Document submission work locally through the whole acceptance path: validate the request and **Product safety limits**, store the **Source file** on disk, create a queued **Extraction job** in local **Workspace product data**, and schedule the **Local extraction runner**. This slice should not complete extraction yet; it proves local acceptance and queueing without Cloudflare R2 or Queue.

## Acceptance criteria

- [x] Document submission uses the `document` multipart field and accepted MIME types only.
- [x] Maximum **Source file** size and PDF **Source file page count** validation happen before durable side effects where possible.
- [x] Accepted **Source files** are written to local Source file storage under local runtime state.
- [x] Queued **Extraction jobs** are created in the local Workspace product store with Source file metadata and selected **Template version**.
- [x] The local runner is scheduled or notified after queued job creation.
- [x] If product-store job creation fails after Source file write, the local Source file is deleted.
- [x] If local runner scheduling fails synchronously, the job is marked failed and the local Source file is deleted.
- [x] Frontend document upload shows queued jobs and **Document upload toast** behavior without billing copy.
- [x] Tests cover successful queued submission and failure cleanup paths.

## Blocked by

- .scratch/bun-local-runtime-refactor/issues/08-retire-billing-from-product-workflows.md
- .scratch/bun-local-runtime-refactor/issues/09-template-editing-and-shape-constraints.md

## Comments

> *This was generated by AI during triage.*

Completed local Document acceptance and queueing. The authenticated `/v1/extract` route validates the existing `document` multipart contract and Product safety limits, writes accepted Source files below `.local/source-files`, records Source file metadata and the selected Template version with a queued Extraction job in per-Workspace SQLite, and notifies the local queue seam for the next runner slice. Local job list/detail reads now expose queued work to the existing frontend upload flow.

Source-file cleanup is covered when queued-job persistence fails, and synchronous queue notification failures mark the job failed before deleting its binary. The source store itself has write/read/delete coverage, while invalid MIME types, oversize inputs, and malformed PDFs are rejected before storage.

Verification passed: backend `bun run test` (396 Vitest and 46 Bun tests) plus `bun run typecheck`; frontend `bun run test` (220 tests) and `bun run build`.
