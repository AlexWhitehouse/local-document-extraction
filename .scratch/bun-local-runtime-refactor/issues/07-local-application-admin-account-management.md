# Local Application Admin Account Management

Category: enhancement
Status: completed

## Parent

.scratch/bun-local-runtime-refactor/PRD.md

## What to build

Keep **Application admin** account management available in the local Bun runtime. Application admins should be able to list/search users, change application roles, ban/unban users, and impersonate eligible regular users using local auth/control persistence. This slice should preserve account-level admin behavior without introducing billing or Workspace product management.

## Acceptance criteria

- [x] **Application admin** visibility is based on persisted application role in the local authenticated session.
- [x] Application admins can list and search users through Better Auth admin behavior in the Bun runtime.
- [x] Application admins can promote/demote eligible users with existing self-action safeguards.
- [x] Application admins can ban and unban users with required reasons.
- [x] Application admins can impersonate eligible regular users and stop impersonation.
- [x] Non-admin users cannot access the **Application admin page** or admin actions.
- [x] Frontend admin page behavior remains available during Workspace loading or resolution error states.
- [x] Tests cover admin visibility, role changes, ban/unban, impersonation safeguards, and frontend transitions.

## Blocked by

- .scratch/bun-local-runtime-refactor/issues/02-local-auth-and-mail-sink.md

## Comments

> *This was generated by AI during triage.*

Implemented local Application admin account management on Better Auth with persisted `admin`/`user` roles, configured local admin bootstrap emails, server-side self-role and role-vocabulary safeguards, and required ban reasons. Better Auth provides the user list/search, authorization, ban/unban, and impersonation session lifecycle under Bun SQLite.

Verification passed: `bun test src/localAuth.bun.test.ts src/localAuthRuntime.bun.test.ts`, `bun run typecheck`, the focused Application admin frontend suite (16 tests), and `bun run build` in `frontend`.
