# Local Extraction Runner With LiteLLM Completion

Category: enhancement
Status: completed

## Parent

.scratch/bun-local-runtime-refactor/PRD.md

## What to build

Implement the first complete local extraction path. The **Local extraction runner** claims queued **Extraction jobs**, reads local **Source files**, sends the original Source file to the configured LiteLLM **Model gateway**, normalizes results, persists **Extraction results**, marks jobs completed or failed, and cleans up Source file binaries after successful completion.

## Acceptance criteria

- [x] The **Local extraction runner** claims eligible queued jobs and moves them to `processing`.
- [x] The runner reads **Source files** from local Source file storage and sends them to the configured LiteLLM **Model gateway**.
- [x] Model results are normalized against the submitted **Template version** fields.
- [x] Completion persists **Extraction results** and marks the **Extraction job** `completed` in one coherent product operation.
- [x] Permanent processing failures mark the **Extraction job** `failed` with durable error information.
- [x] Successful completion attempts Source file cleanup without corrupting completed results if cleanup fails.
- [x] Completed and failed jobs are visible through existing job list/detail API behavior.
- [x] Tests cover successful completion, failed processing, missing Source file, result persistence before completion, and cleanup behavior.

## Blocked by

- .scratch/bun-local-runtime-refactor/issues/10-local-source-file-store-and-queued-document-submission.md

## Comments

> *This was generated by AI during triage.*

Completed the first local extraction path. The in-process local queue notifies a non-blocking runner after durable queueing; the runner claims the SQLite job, reads its local Source file, uses the configured OpenAI-compatible LiteLLM gateway, normalizes every result against the submitted Template version, persists results and completion atomically, and then attempts Source file cleanup. The Model gateway client now accepts a runtime-neutral configuration object so Bun can use the same gateway behavior without Cloudflare bindings.

Missing Source files and processing failures are persisted as failed jobs with durable error details. Cleanup failures leave completed results intact and retain the binary for later recovery. Local job detail reads now return persisted results for the existing Documents UI.

Verification passed: backend `bun run test` (396 Vitest and 50 Bun tests) plus `bun run typecheck`; frontend `bun run test` (220 tests) and `bun run build`.
