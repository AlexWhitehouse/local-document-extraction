# Local Workspace Product Store Template Create/List

Category: enhancement
Status: completed

## Parent

.scratch/bun-local-runtime-refactor/PRD.md

## What to build

Create the first **Workspace product data** vertical slice on local SQLite: each Workspace gets a local product database, and Template creation/listing reads and writes through that Workspace product store after **Workspace control data** authorization succeeds. This proves the local authority split without waiting for the full extraction path.

## Acceptance criteria

- [ ] Local product migrations create the current Template and Template field schema for a per-Workspace product database.
- [ ] Product migrations are idempotent and do not replay historical global D1 product table migrations.
- [ ] `POST /v1/templates` authenticates and authorizes through local **Workspace control data** before writing to local **Workspace product data**.
- [ ] `GET /v1/templates` returns Templates from the local Workspace product store.
- [ ] Newly bootstrapped starter Templates are visible through the local product-store-backed Template list path.
- [ ] Session-authenticated product requests continue to work where they worked before.
- [ ] Tests cover successful Template creation/listing, authorization before product routing, and Workspace isolation.

## Blocked by

- .scratch/bun-local-runtime-refactor/issues/03-local-workspace-control-and-accepted-context.md

## Comments

> *This was generated by AI during triage.*

Completed with a fresh per-Workspace Bun SQLite product store at `.local/data/workspaces/<workspace-id>.sqlite`. The local API now authorizes accepted Workspace context through control data before opening the product store, lists bootstrap starter Templates, and creates/lists user Templates. Focused tests cover database isolation, starter visibility, authorized create/list behavior, and rejection of an unauthorized Workspace request before product routing. The combined local Bun suite (13 tests), backend typecheck, frontend accepted-context tests, and frontend build pass.
