# 06 — Use native base64 for Model gateway payloads

**What to build:** Prepare inline Source file and rendered-page Model gateway payloads through Bun's native Buffer encoding path, eliminating the JavaScript binary-string loop without changing the OpenAI-compatible request contract.

**Blocked by:** None — can start immediately.

**Category:** enhancement

**Status:** completed

- [x] Empty, odd-sized, boundary-sized, multi-megabyte, image, and PDF-page inputs produce byte-for-byte identical base64 output before and after the change.
- [x] Generated data URLs retain the same media types, prefixes, ordering, and Model gateway request shape.
- [x] The manual binary-string accumulation and browser-style base64 call are no longer used for Model gateway payload preparation.
- [x] Focused benchmarks record median encoding time and peak memory for representative 2, 5, and 10 MiB inputs using both implementations.
- [x] An end-to-end Model gateway fixture confirms unchanged extraction behaviour and records preparation latency/RSS at realistic page concurrency.
- [x] Model gateway and complete backend tests pass under both the current and pinned Bun runtime during the transition.

## Comments

> *This was generated by AI during triage.*
>
> ## Agent Brief
>
> **Category:** enhancement  
> **Summary:** Replace Model gateway payloads' chunked JavaScript/btoa encoder with Bun/Node's native Buffer base64 path while preserving every request byte.
>
> **Current behavior:** `modelGateway.ts` builds a binary string in 32 KiB chunks and calls `btoa`. Every inline PDF, image, and rendered page pays JavaScript allocation/copy overhead before JSON request serialization.
>
> **Desired behavior:** One small encoding module owns native base64 conversion; the gateway uses it for all inline content. Equivalence tests cover edge and multi-MiB sizes, existing request-shape tests remain unchanged, and a reproducible benchmark records native versus legacy latency/memory plus an end-to-end preparation run.
>
> **Key interfaces:** `encodeModelPayloadBase64(ArrayBuffer)`; inline file and image data URLs produced by `runExtraction`; the gateway `fetch` request body; benchmark output.
>
> **Acceptance criteria:**
>
> - [x] Equivalence tests compare legacy and native output for empty, odd, chunk-boundary, and multi-MiB deterministic inputs.
> - [x] Existing PDF/image gateway fixtures prove media types, prefixes, ordering, and request JSON are unchanged.
> - [x] A reproducible 2/5/10 MiB benchmark records median duration and peak RSS for both encoders plus realistic concurrent gateway preparation.
> - [x] No production manual binary-string loop or `btoa` remains.
> - [x] Focused and complete backend tests pass under Bun 1.3.14 and pinned Bun 1.4.0.
>
> **Out of scope:** changing model provider schemas, upload policy, PDF rendering, or transport concurrency.

> *This was generated by AI during triage.*
>
> Completed. All inline PDF, source-image, and rendered-page data URLs now use the isolated native `Buffer.from(...).toString("base64")` encoder. Eight deterministic equivalence cases cover empty, odd, 32 KiB boundaries, and multi-MiB inputs; all existing gateway request-shape fixtures remain byte-for-byte green. The reproducible benchmark and recorded evidence show 159–170× lower median encoding time across 2/5/10 MiB inputs, with lower sampled RSS deltas; four concurrent 2 MiB gateway preparations completed in 3.02 ms. Complete backend suites passed under Bun 1.3.14 and 1.4.0, and the full pinned-runtime gate passed with 416 tests and production build. Evidence: `../evidence/06-model-payload-base64-benchmark.md`.
