# 05 — Resolve the ExcelJS/UUID advisory path

**What to build:** Upgrade or replace the affected spreadsheet dependency path so users can still download trustworthy Workspace Extraction job exports without retaining the reported UUID advisory.

**Blocked by:** 03 — Apply range-compatible dependency security fixes.

**Category:** enhancement

**Status:** completed

- [x] A fresh Bun audit no longer reports the current ExcelJS/UUID advisory path, or an explicit reachability assessment demonstrates that the vulnerable operation is absent and records an owner and removal deadline.
- [x] Workspace export still produces a readable XLSX workbook with the established worksheets, headings, field values, table-shaped Template results, and filename contract.
- [x] Empty, large, Unicode, formula-like, and malformed result values are exported without corruption or unintended formula execution.
- [x] The browser download flow preserves the response media type and server-provided filename.
- [x] Existing export tests and the complete root command surface pass under Bun 1.4.0.
- [x] The dependency diff contains only the spreadsheet/security decision and its unavoidable transitive changes.

## Comments

> *This was generated by AI during triage.*
>
> ## Agent Brief
>
> **Category:** enhancement  
> **Summary:** Remove ExcelJS's vulnerable transitive UUID installation with the smallest compatible resolution change and harden the workbook boundary for adversarial result values.
>
> **Current behavior:** `exceljs@4.4.0` is the latest published ExcelJS but declares `uuid@^8.3.0`, leaving Bun's sole remaining advisory. Workbook generation and HTTP download are tested, but the value suite does not explicitly cover formula-like strings, oversized Excel cells, or non-JSON-native object members.
>
> **Desired behavior:** A root resolution override supplies a fixed UUID implementation while ExcelJS workbook create/load remains compatible. Text remains typed as text, oversized values are bounded without corrupting Unicode, malformed structured values serialize safely, and the HTTP media type/filename contract is unchanged.
>
> **Key interfaces:** the root dependency resolution graph; `buildJobExportWorkbook`; XLSX create/load round trip; `POST /v1/jobs/export` response headers and bytes.
>
> **Acceptance criteria:**
>
> - [x] A dependency-contract regression enforces `uuid >=11.1.1`, and a fresh audit reports zero findings.
> - [x] The existing workbook shape and HTTP download tests pass with the overridden UUID implementation.
> - [x] New round-trip coverage proves formula-like text stays text, Unicode survives, oversized cells are safely bounded, and malformed structured values do not abort the export.
> - [x] The frozen install is stable and the complete Bun 1.4 gate passes.
> - [x] Package diff/lock review contains only the UUID resolution and unavoidable removal of the old version.
>
> **Out of scope:** replacing the spreadsheet format or UI; changing worksheet layouts; general package policy from ticket 20.

> *This was generated by AI during triage.*
>
> Completed. Because `exceljs@4.4.0` remains the latest release and still declares UUID 8, the root graph now narrowly overrides UUID to the first audited fixed release, `11.1.1`; ExcelJS's CommonJS `v4` use remains compatible and is exercised by create/load and HTTP export tests. Formula-like values round-trip as text, Unicode is preserved, Excel's 32,767-character cell limit is enforced with a visible truncation marker, and malformed BigInt-containing structures serialize without aborting the export. `bun pm diff` and the lockfile were reviewed, `bun audit` now returns `{}`, the frozen install is unchanged, and the full Bun 1.4 gate passed with 408 tests and the production build.
