# 20 — Enforce Bun package hygiene

**What to build:** Make dependency installation and review reproducible, fast across worktrees, and resistant to known supply-chain risks using Bun's frozen install, audit, dedupe, diff, license, and global-store capabilities.

**Blocked by:** 03 — Apply range-compatible dependency security fixes; 04 — Upgrade the vulnerable Vite/esbuild dependency line; 05 — Resolve the ExcelJS/UUID advisory path.

**Category:** enhancement

**Status:** completed

- [x] Clean CI installs use Bun's frozen CI command and prove that a successful install does not modify the lockfile.
- [x] CI runs non-mutating production audit and dedupe checks, reports actionable failures, and never applies automatic dependency fixes.
- [x] The currently identified compatible duplicate packages are removed in a reviewed dependency-maintenance change.
- [x] Sensitive auth, parsing, native-binary, and networking upgrades include Bun package-diff evidence covering new files, install scripts, and sensitive imports.
- [x] A machine-readable production license inventory is generated as a release/CI artifact.
- [x] Warm-cache time and disk use are compared across two clean worktrees, and native canvas optional binaries plus offline/frozen installs remain correct with the global isolated store.
- [x] A minimum-release-age policy is either adopted with a documented window and narrow exceptions or explicitly rejected with a recorded release-workflow rationale.

## Comments

> *This was generated by AI during triage.*
>
> ## Agent Brief
>
> **Category:** enhancement  
> **Summary:** Make the now-audit-clean Bun dependency graph reproducible and reviewable: frozen CI installs, non-mutating audit/dedupe gates, normalized production-license artifacts, reviewed sensitive-package diffs, a measured global isolated-store workflow, and a narrow release-age policy.
>
> **Current behavior:** CI uses `bun install --frozen-lockfile`, but does not use Bun's CI alias or explicitly prove the lockfile stayed unchanged. The production audit is currently clean, while `bun dedupe --check` still reports removable `safe-buffer` and `string_decoder` copies. No package-hygiene artifact or license inventory is uploaded. The prior parser, build-tool, UUID, and networking upgrades have no unified `bun pm diff --json` evidence. The isolated linker is selected by the Bun lock format, but Bun 1.4's opt-in global virtual store is not enabled or measured, and no minimum-release-age policy exists.
>
> **Desired behavior:** Add a project-owned, non-mutating package-hygiene command that runs with no project environment, captures production audit JSON, requires `dedupe --check` to pass, normalizes production license JSON into a portable machine-readable artifact, and emits an actionable Markdown summary. CI installs with `bun ci`, proves `bun.lock` is unchanged, runs and uploads the hygiene evidence, and never invokes `audit fix` or mutating `dedupe`. Perform the currently reviewed compatible dedupe once and keep its resulting lockfile diff. Generate JSON package-diff evidence for the actual sensitive version transitions already made. Explicitly configure isolated/global-store installs plus a three-day release cooling period with only `bun-types` exempt so runtime/type qualification can stay version-locked. Measure two clean dependency worktrees against one warm cache/store, deny the registry during the second frozen install, verify global-store links and native canvas execution, and record time/disk/lock/cleanliness outcomes.
>
> **Key interfaces:** `bun ci`; `bun audit --prod --json`; `bun dedupe --check`; `bun pm licenses --prod --json`; `bun pm diff … --json`; `bunfig.toml` install policy; root package scripts; CI install/artifact steps; the `node_modules/.bun` → global-cache link layout; native `@napi-rs/canvas` smoke.
>
> **Acceptance criteria:**
>
> - [ ] Every CI checkout installs with `bun ci` and immediately proves `bun.lock` has no diff.
> - [ ] A tested root hygiene command runs audit, dedupe-check, and production-license inventory non-mutatively with bounded diagnostics; an advisory or removable compatible duplicate fails the command.
> - [ ] The reviewed one-time `bun dedupe` removes the reported `safe-buffer` and `string_decoder` duplicates, after which `dedupe --check`, production audit, frozen install, and the product test surface pass.
> - [ ] Portable artifacts include audit JSON, normalized production license JSON, a Markdown summary, and JSON `bun pm diff` evidence for `pdfjs-dist` 6.1.200→6.2.108, Vite 5.4.21→8.2.2, UUID 8.3.2→11.1.1, and Undici 7.28.0→7.29.0; unchanged Better Auth and native-canvas versions are explicitly recorded rather than implied to have upgraded.
> - [ ] Bun installs use the isolated linker and global store. A repeatable two-clean-worktree benchmark records warm-cache/global-store time and disk use, proves the second frozen install succeeds with registry access denied, finds global-store symlinks, leaves both worktrees/lockfiles clean, and runs a native canvas PNG smoke in both.
> - [ ] A three-day (`259200` seconds) minimum release age is documented and configured, with only `bun-types` excluded for exact Bun runtime/type qualification; urgent exceptions require explicit reviewed CLI override and package-diff/audit evidence.
> - [ ] Tests pin artifact normalization, command policy, CI wiring, sensitive-package review coverage, global-store policy, and benchmark rendering before the implementation is accepted; full Bun 1.4 CI and Chromium journey remain green.
>
> **Out of scope:** Automatically applying audit fixes, automatically updating dependencies, publishing artifacts externally, clearing or pruning the user's global Bun cache, enforcing license allow/deny policy without a legal decision, claiming Bun's published 7× result for this repository, changing auth/parser/runtime behavior, or committing/pushing the dependency maintenance work.

Implemented and verified on Bun 1.4.0. All three CI checkout jobs now use `bun ci` and immediately fail on a `bun.lock` diff. The quality and scheduled/manual lanes run and upload one tested, non-mutating package-hygiene command: production audit JSON, `dedupe --check`, normalized production-license JSON, and a bounded Markdown summary. The one-time reviewed lockfile-only dedupe removed only the compatible `safe-buffer@5.2.1` and `string_decoder@1.3.0` copies; the installed graph now covers 392 packages with no removable duplicates and the production audit is empty. The current license artifact inventories 214 production packages across 15 license expressions with portable `<repo>` paths.

The repository now explicitly selects Bun's isolated linker/global store and a three-day release cooling period, with only exact runtime-matched `bun-types` exempt; the urgent override workflow and review requirements are documented. Registry `bun pm diff --json` evidence covers PDF.js, Vite, UUID, and Undici transitions, including Bun's install-script/import/executable notes; unchanged Better Auth and native-canvas boundaries are explicit. Complete normalized patches stay under ignored raw scratch state, while bounded file/status/count/hash evidence and the review summary live at `../evidence/20-package-diffs/`. The repeatable disposable-worktree benchmark recorded 359 global links in each clean worktree, a 349.95 ms store-population install versus 59.45 ms store reuse with the registry denied, 95 KiB local link trees, identical locks, clean Git state, and 97-byte native canvas PNGs; see `../evidence/20-global-store-worktrees.md`. Seven focused TDD tests cover policy and artifacts.

Root CI passed with 216 parallel backend tests plus one real-process smoke test, 250 frontend tests, coverage and build. The Chromium journey and package-hygiene command passed. A final `bun ci` checked 362 installs across 392 packages in 61 ms with no changes and identical pre/post lockfile SHA-256 `8770c23c29d9c9024027a9b468f8a471e5bdfb0e9494879ebb60b0d8f424757f`.
