# 14 — Add a Playwright Extraction job journey

**What to build:** Prove one critical Document Extraction journey in a real Chromium browser running against an isolated Local Bun Runtime and fake Model gateway, with evidence an agent can inspect when any layer fails.

**Blocked by:** 02 — Pin and qualify Bun 1.4.0.

**Category:** enhancement

**Status:** completed

- [x] The journey creates or accesses an account through the supported local auth path, resolves an Accepted workspace context, and creates a usable Template.
- [x] It submits a representative Document, observes the Extraction job lifecycle through a Workspace live update, and verifies the completed Extraction results shown in the UI.
- [x] The test uses isolated temporary runtime state, the Local mail sink where verification is required, and a deterministic fake Model gateway with no external network dependency.
- [x] Failure artifacts include a Playwright trace, screenshot, browser console messages, and relevant network request/response metadata with secrets removed.
- [x] The browser and all child runtime processes are cleaned up after success, assertion failure, or setup failure.
- [x] The journey runs against the built SPA in CI after the production build and remains separate from fast jsdom component tests.

## Comments

> *This was generated by AI during triage.*
>
> ## Agent Brief
>
> **Category:** enhancement  
> **Summary:** Add one deterministic Playwright/Chromium acceptance journey over the production-built SPA, a child Local Bun Runtime, the Local mail sink, and a fake Model gateway; preserve sanitized, agent-readable browser evidence when any layer fails.
>
> **Current behavior:** Backend smoke tests prove the HTTP product path and jsdom tests prove UI controllers, but no test crosses the real browser, built SPA, auth cookies, Workspace live socket, upload form, extraction worker, and rendered results. A browser regression can therefore pass both existing lanes, and failures do not provide trace/console/network evidence.
>
> **Desired behavior:** A sequential Chromium test launches an isolated Bun harness, signs up through the visible auth UI, consumes the captured verification link, signs in, observes an Accepted starter Workspace, creates an invoice Template, uploads a representative source file, proves a completed lifecycle frame arrived through the browser's Workspace WebSocket, and asserts the fake gateway result in Document Details. Playwright retains trace and screenshot on failure and always attaches sanitized console/network/live-frame metadata. Every child and temporary directory is disposed on setup failure, assertion failure, and success.
>
> **Key interfaces:** `@playwright/test`; production `frontend/dist`; `backend/src/server.ts` ready event and graceful signal path; Local mail JSONL; `/api/auth/*`; `/v1/templates`; `/v1/extract`; `/v1/workspaces/:id/live`; Playwright page request/response/console/WebSocket events; GitHub Actions browser installation and artifact upload.
>
> **Acceptance criteria:**
>
> - [x] The first acceptance run fails before the Playwright dependency/harness is implemented, then the completed browser journey passes under Bun 1.4.0.
> - [x] The visible auth flow creates and verifies an account through isolated Local mail, signs in, and renders an Accepted starter Workspace.
> - [x] The visible Template and upload controls create a valid invoice Template and submit a representative PNG without external requests.
> - [x] A browser-observed Workspace live frame reports the job completed and the UI renders `INV-E2E-001` in Document Details.
> - [x] Failure output retains a trace and screenshot; console, relevant HTTP metadata, and bounded/redacted live-frame metadata are attached without cookies, authorization values, verification query tokens, document bytes, or model request/response bodies.
> - [x] The harness has bounded readiness/shutdown diagnostics, owns an isolated temporary state directory and loopback fake gateway, and reaps all children/state on every outcome.
> - [x] CI installs only Chromium, builds the production SPA first, runs the browser lane separately from root/jsdom tests with one worker, and uploads its report/results even on failure.
>
> **Out of scope:** multi-browser coverage, visual snapshots, production OAuth, external Model calls, browser performance budgets, and replacing backend protocol smoke coverage.

> *This was generated by AI during implementation.*
>
> Completed with pinned `@playwright/test` 1.62.1 and a one-worker Chromium project over the production SPA. The visible journey signs up, consumes the isolated Local mail verification action outside the browser trace, signs in to an API-ready Accepted starter Workspace, creates an invoice Template through the JSON UI, uploads a valid PNG, observes queued/processing/completed Workspace frames in Chromium, and renders `INV-E2E-001`. A Bun 1.4.0 child harness owns the loopback fake gateway/runtime and temporary state; readiness validates runtime version and loopback origins, shutdown is bounded through a loopback control request, the page closes before runtime teardown, parent-loss is watched, and the test asserts state removal. Browser HTTP is isolated from the public network. Stable query-free console/network/frame evidence and a final screenshot are emitted on every run; `trace.zip` is retained on failure. A deliberate terminal assertion failure produced all five requested artifacts, and its JSON artifacts matched none of the secret/content patterns. CI now builds first, installs only Chromium, runs this lane separately from the 250-test jsdom suite, and always uploads `.scratch/ci/playwright`. Frozen install did not change the lock, the 428-test root gate passed, and the final three evidence unit tests plus Chromium journey passed under Bun 1.4.0.
