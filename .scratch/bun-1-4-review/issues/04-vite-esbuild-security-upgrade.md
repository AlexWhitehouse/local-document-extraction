# 04 — Upgrade the vulnerable Vite/esbuild dependency line

**What to build:** Move the frontend development and production build onto a supported Vite/esbuild line that removes the outstanding advisory path without changing the SPA's routing, proxy, or browser behaviour.

**Blocked by:** 03 — Apply range-compatible dependency security fixes.

**Category:** enhancement

**Status:** completed

- [x] The direct frontend Vite dependency no longer resolves an affected Vite/esbuild path according to a fresh Bun audit.
- [x] Duplicate Vite generations introduced by build and test tooling are removed where their supported ranges permit, or their continued presence is documented and audited.
- [x] The development server still proxies auth and workspace-scoped product routes to the Local Bun Runtime.
- [x] Production build, direct SPA navigation, asset loading, and SPA fallback remain correct.
- [x] All frontend unit/component tests and the complete root checks pass.
- [x] Windows-specific path-denial behaviour covered by the advisory is exercised by an automated or CI-platform-specific regression check where practical.

## Comments

> *This was generated by AI during triage.*
>
> ## Agent Brief
>
> **Category:** enhancement  
> **Summary:** Replace the direct Vite 5/esbuild 0.21 line with the current Vite line already supported by Vitest, eliminating the audited development-server path while preserving the SPA contract.
>
> **Current behavior:** The frontend resolves direct `vite@5.4.21`/`esbuild@0.21.5`, while `vitest@4.1.10` independently resolves Vite 8.1.x. Bun reports the direct Vite/esbuild line as vulnerable, and the two generations duplicate build infrastructure.
>
> **Desired behavior:** Frontend build/dev and Vitest share one non-vulnerable Vite generation. The `/api/auth` and `/v1` proxies, WebSocket proxying, built assets, and backend SPA fallback retain their current behavior.
>
> **Key interfaces:** `frontend/package.json`; the installed Vite graph; `frontend/vite.config.js`; frontend build output; the backend built-SPA smoke path.
>
> **Acceptance criteria:**
>
> - [x] An automated dependency-contract test proves the installed Vite floor and that frontend/Vitest resolve one Vite version.
> - [x] The config regression proves both API proxy targets and `/v1` WebSocket forwarding remain enabled.
> - [x] A fresh audit contains no Vite or esbuild finding.
> - [x] Frontend tests/build and the complete root Bun 1.4 gate pass, including built-SPA fallback smoke.
> - [x] The Windows path-denial advisory is covered by the fixed-version contract; adding Windows runtime support remains outside the repository's declared Linux/macOS matrix.
>
> **Out of scope:** frontend feature or style changes; Playwright coverage from ticket 14; permanent audit policy from ticket 20; claiming Windows as a supported Local Bun Runtime target.

> *This was generated by AI during triage.*
>
> Completed. The direct frontend moved from Vite 5.4.21/esbuild 0.21.5 to Vite 8.2.2/Rolldown, and Bun's transitive update support deduplicated Vitest onto that same Vite installation. A dependency-contract regression enforces both the audited `>=6.4.3` floor and single-version graph; the extracted proxy contract remains consumed by `vite.config.js` and verifies auth, product API, and WebSocket routing. `bun pm diff` was reviewed, the fresh audit contains neither Vite nor esbuild, the frozen install is stable, and the full Bun 1.4 gate passed with 406 tests and a Vite 8 production build. The fixed-version check covers the Windows advisory without expanding the declared runtime matrix to Windows.
