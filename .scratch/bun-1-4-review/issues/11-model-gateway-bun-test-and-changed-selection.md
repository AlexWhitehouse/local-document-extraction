# 11 — Move the Model gateway test and enable changed-test selection

**What to build:** Bring the exceptional Model gateway test into the Bun-native backend graph with explicit global restoration and bounded asynchronous waiting, then make import-aware changed-test selection reliable across the complete backend.

**Blocked by:** 10 — Move simple backend tests from Vitest to bun:test.

**Category:** enhancement

**Status:** completed

- [x] Global replacements used by Model gateway tests are captured and restored explicitly even when setup or assertions fail.
- [x] Polling assertions use a small bounded diagnostic waiter or explicit completion signal with equivalent timeout behaviour.
- [x] Every existing Model gateway scenario runs under `bun:test` with the same request, retry, timeout, error-classification, and managed-file assertions.
- [x] The backend test command uses direct Bun discovery and no longer shells out to run a second test framework or construct a file list.
- [x] A Model gateway source change selects its affected tests, a Workspace product store change selects its affected tests, and a documentation-only change selects no unrelated backend tests.
- [x] Focused changed-test selection remains a development/agent shortcut; the complete backend lane is still required before handoff.
- [x] Backend-only Vitest dependencies and configuration are removed when no backend caller remains, without changing frontend Vitest.

## Comments

> *This was generated by AI during triage.*
>
> ## Agent Brief
>
> **Category:** enhancement  
> **Summary:** Migrate the final 28-scenario Model gateway suite to `bun:test`, explicitly own its `fetch` replacement and asynchronous wait contract, then expose Bun's import-aware changed-test shortcut without weakening the complete handoff gate.
>
> **Current behavior:** `modelGateway.test.ts` is the backend's only Vitest file. It uses `vi.stubGlobal`, `vi.unstubAllGlobals`, `vi.fn`, and one `vi.waitFor`, so the backend command shells through Vitest before running the native lane. No project command documents or verifies Bun's `--changed` selection.
>
> **Desired behavior:** The suite is discovered directly by Bun as `modelGateway.bun.test.ts`; every replacement of `globalThis.fetch` restores the captured original in `afterEach`; the sequential-call assertion uses a bounded waiter whose timeout reports the expected and observed call counts. The backend has no direct Vitest dependency, provides a `test:bun:changed` shortcut, and retains the full native lane as the handoff requirement.
>
> **Key interfaces:** `globalThis.fetch`; Bun mock call history; the sequential-call synchronization point; backend package scripts and direct discovery; Git/import-aware `bun test --changed`; root/frontend dependency boundaries.
>
> **Acceptance criteria:**
>
> - [x] Characterization preserves all 28 Model gateway scenarios and their existing request, retry, timeout, error, cancellation, and managed-file assertions.
> - [x] A captured `fetch` reference is restored after every test, including rejected assertions, and no Vitest global-stub API remains.
> - [x] The only polling assertion uses a bounded diagnostic waiter and fails with useful expected/observed call information.
> - [x] Direct Bun discovery reports the same 174 backend tests and all native serial/parallel lanes pass.
> - [x] A checked changed-selection contract covers Model gateway source, Workspace product-store source, and documentation-only changes; the full backend lane remains separate and required.
> - [x] Backend Vitest scripts/dependency are removed, frontend Vitest remains green, and the complete Bun 1.4 root gate passes.
>
> **Out of scope:** merged CI artifact/reporting policy (ticket 09), frontend test-runner migration, and changing Model gateway production behavior.

> *This was generated by AI during implementation.*
>
> Completed with all 28 Model gateway scenarios under `bun:test`. The suite captures the original `globalThis.fetch`, restores it from `afterEach` after every test outcome, and replaces the lone `vi.waitFor` with a 1-second waiter that reports expected and observed calls. Direct discovery now runs 173 isolated non-smoke tests plus the serial smoke test (174 unchanged), and backend Vitest scripts/dependency are gone while frontend Vitest remains at 245 passing tests. `test:bun:changed` is a shortcut only; `test:bun:verify-changed` recorded 8 affected files for a Model gateway edit, 24 for a Workspace product-store edit, and none for documentation-only input in `evidence/11-changed-test-selection.md`. The complete Bun 1.4 gate passed typecheck, lint, all 419 tests, build, and `git diff --check`.
