# 10 — Move simple backend tests from Vitest to bun:test

**What to build:** Migrate the backend tests that use ordinary describe, assertion, and mock behaviour into the Bun-native graph in green batches, while preserving frontend Vitest and leaving the exceptional Model gateway test for its dedicated slice.

**Blocked by:** 08 — Run Bun-native backend tests isolated and capped-parallel.

**Category:** enhancement

**Status:** completed

- [x] Every backend Vitest file that does not depend on unsupported global-stubbing or polling helpers runs under `bun:test` with equivalent assertions and failure diagnostics.
- [x] Migration is performed in independently green batches; no batch silently drops, skips, or weakens an existing test.
- [x] The Bun-native test count increases by exactly the number of migrated tests while the combined backend count remains unchanged.
- [x] Migrated files pass under serial, isolated, capped-parallel, randomized, and coverage lanes.
- [x] Shared test helpers register temporary resources and cleanup at acquisition time and do not depend on cross-file globals.
- [x] Backend Vitest remains only for the explicitly deferred exceptional test, and frontend Vitest configuration and behaviour are unchanged.

## Comments

> *This was generated by AI during triage.*
>
> ## Agent Brief
>
> **Category:** enhancement  
> **Summary:** Move the ten ordinary backend Vitest files into the isolated Bun-native lane without changing their 27 scenarios or assertions, leaving only the global-stubbing Model gateway suite.
>
> **Current behavior:** Eleven backend files contribute 55 Vitest tests. Ten files use only ordinary describe/test/assertion behavior; `modelGateway.test.ts` contributes the 28 exceptional scenarios using Vitest global stubs, function mocks, and polling.
>
> **Desired behavior:** The ten simple files use `bun:test`, follow the `.bun.test.ts` discovery convention, and pass every native lane. The Vitest command targets only Model gateway. Combined backend totals remain unchanged after each green batch.
>
> **Key interfaces:** test imports and filenames; `test:bun:*` discovery; `test:vitest`; serial, parallel, random, and coverage-compatible Bun execution.
>
> **Acceptance criteria:**
>
> - [x] Characterization baseline records 55 Vitest + 119 Bun-native = 174 backend tests before migration.
> - [x] Three independently green batches migrate exactly 27 simple tests with no assertion edits or skips.
> - [x] After migration, Vitest reports only the 28 Model gateway scenarios and Bun reports 146 tests; combined total remains 174.
> - [x] Migrated tests pass isolated serial, two-worker parallel, seeded random, and coverage lanes.
> - [x] Frontend Vitest is untouched and the complete root gate passes.
>
> **Out of scope:** adapting Model gateway mocks/polling (ticket 11), removing backend Vitest dependencies (ticket 11), reporting policy (ticket 09).

> *This was generated by AI during implementation.*
>
> Completed in three green batches without changing test bodies, assertions, or skip policy. The native lane moved from 119 to 146 tests (+27), Vitest moved from 55 to the 28-scenario Model gateway exception, and the combined backend count stayed at 174. The migrated set passed isolated serial (27 tests, 83 assertions), two-worker capped-parallel, seeded random (`101027`), and Bun coverage execution. The final Bun 1.4 root gate passed 174 backend plus 245 frontend tests (419 total), typecheck, lint, and the Vite production build; frontend Vitest was unchanged.
