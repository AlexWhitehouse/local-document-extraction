# 12 — Centralize frontend test isolation

**What to build:** Give every frontend component/controller test the same reliable browser-state boundary so agents do not mistake leaked DOM, storage, mocks, timers, or real-clock races for product failures.

**Blocked by:** None — can start immediately.

**Category:** enhancement

**Status:** completed

- [x] One shared setup boundary cleans rendered DOM, local storage, mocks, fake timers, and stubbed globals after every test.
- [x] Duplicated per-file cleanup is removed only after proving the shared boundary provides equivalent behaviour.
- [x] Real sleeps in component/controller tests are replaced with observable events, injected scheduling, or fake-clock advancement.
- [x] Tests whose filenames or expectations depend on the current day use an injected or frozen clock and always restore it.
- [x] Repeated randomized frontend runs have no duplicate-element, leaked-timer, stale-storage, or midnight-rollover failures.
- [x] Existing frontend behaviour assertions and the complete root checks remain unchanged in meaning and pass.

## Comments

> *This was generated by AI during triage.*
>
> ## Agent Brief
>
> **Category:** enhancement  
> **Summary:** Turn the existing frontend setup file into the single after-test ownership boundary for rendered DOM, storage, globals, mocks, and timers, then remove safe duplicate cleanup and eliminate the remaining real timer sleeps.
>
> **Current behavior:** `src/test/setup.js` only polyfills local storage. Several large suites duplicate `cleanup()`/mock restoration, directly replace `fetch`, `WebSocket`, storage, clipboard, and location, and four controller tests wait 20–200ms on real timers. Fake-timer restoration relies on local `try/finally` blocks.
>
> **Desired behavior:** A shared `afterEach` cleans RTL containers, clears both storage areas, clears/restores mocks and fake clocks, unstubs globals, and restores captured browser/global descriptors even after a failed assertion. Controller tests synchronize on observable calls or deterministic clock advancement. A regression suite proves that deliberately leaked state from one test is absent in the next.
>
> **Key interfaces:** Vitest setup lifecycle; RTL `cleanup`; `window.localStorage`/`sessionStorage`; `globalThis.fetch`, `WebSocket`, and `URL`; `navigator.clipboard`; `window.location`; Vitest mock and timer state.
>
> **Acceptance criteria:**
>
> - [x] A two-test isolation regression leaks DOM, storage, a mock call, a fake timer, a stubbed global, and a spy in test one and observes a clean boundary in test two.
> - [x] Shared cleanup runs after every frontend test outcome and restores captured descriptors explicitly.
> - [x] Redundant per-file cleanup is removed from files not carrying unrelated user edits only after the regression is green.
> - [x] No component/controller test uses a real timer sleep; existing fake-clock tests retain `try/finally` restoration in addition to the global safety net.
> - [x] Repeated randomized runs remain green with the same 245 behavior scenarios plus the isolation regression.
> - [x] The complete Bun 1.4 root gate passes without changing production frontend behavior.
>
> **Out of scope:** frontend coverage policy (ticket 13), browser E2E (ticket 14), and editing unrelated in-progress Template field UI changes.

> *This was generated by AI during implementation.*
>
> Completed with a shared setup boundary that cleans RTL containers, memory-backed local/session storage, fake timers, mock state and implementations, Vitest global stubs, and captured `fetch`/`WebSocket`/`URL`/storage/location/clipboard descriptors. The two-scenario leak regression is green. Redundant cleanup was removed from untouched suites after that proof; the user-modified `TemplateFieldEditor.test.jsx` was deliberately preserved. Four 20–200ms sleeps were replaced by microtask completion or the stronger assertion that the 150ms capacity-refresh timer was never scheduled. Seed `12120` initially exposed a retained `refetchSession` mock implementation; resetting implementations at the shared boundary fixed it, and five shuffled runs (`12120`–`12124`) then passed all 247 frontend tests. No current-day-derived expectation was found. The Bun 1.4 root gate passed 178 backend + 247 frontend tests (425 total), lint, typecheck, build, and `git diff --check`.
