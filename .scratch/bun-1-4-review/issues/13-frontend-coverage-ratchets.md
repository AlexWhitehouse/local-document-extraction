# 13 — Add frontend coverage ratchets

**What to build:** Produce trustworthy frontend coverage evidence that identifies untested production modules and prevents accidental coverage regression without imposing an arbitrary headline percentage.

**Blocked by:** 12 — Centralize frontend test isolation.

**Category:** enhancement

**Status:** completed

- [x] The frontend test lane emits both human-readable coverage and LCOV suitable for CI and agent inspection.
- [x] Production frontend modules missing entirely from LCOV are listed explicitly rather than disappearing from the denominator.
- [x] Initial line, function, and branch baselines are measured from the complete suite and recorded at or just below observed values.
- [x] Thresholds fail a deliberate regression and can only be lowered through an explicit reviewed change.
- [x] Coverage artifacts are uploaded on CI failure as well as success and contain no browser storage secrets or captured Document content.
- [x] The normal fast test command remains available without coverage, while the required release/CI lane exercises the coverage gate.

## Comments

> *This was generated by AI during triage.*
>
> ## Agent Brief
>
> **Category:** enhancement  
> **Summary:** Add a V8 coverage lane around the now-isolated Vitest suite, include every production JS/JSX module in its denominator, emit LCOV plus a concise Markdown gap report, and enforce measured line/function/branch floors without auto-update.
>
> **Current behavior:** The 247-test frontend suite has no coverage provider, artifacts, all-source include policy, or threshold. CI can pass while an unimported production module silently disappears from coverage.
>
> **Desired behavior:** `test` stays fast. `test:coverage` runs the complete suite with text, LCOV, and JSON-summary reporters, `reportOnFailure`, explicit production includes, and reviewed thresholds read from a baseline file. A postprocessor compares discovered production files with LCOV, reports absent and zero-covered modules, and writes stable evidence under `.scratch/ci/frontend`.
>
> **Key interfaces:** `@vitest/coverage-v8`; Vitest coverage includes/excludes, reporters, `reportOnFailure`, and thresholds; LCOV `SF` records; JSON summary totals; root `ci`; CI always-upload artifact steps.
>
> **Acceptance criteria:**
>
> - [x] Coverage helper regression tests parse line/function/branch totals, detect absent modules, and reject a deliberate below-baseline sample.
> - [x] The complete suite emits a human table, LCOV, JSON summary, and Markdown listing modules absent from LCOV and modules with zero covered lines.
> - [x] Checked baselines are set at or just below an observed complete-suite run and `autoUpdate` is never enabled.
> - [x] Fast `test` remains coverage-free; root `ci` and `test:coverage` enforce the gate.
> - [x] CI regenerates and uploads LCOV/summary evidence with `if: always()` even if an earlier step or the coverage tests fail.
> - [x] Uploaded coverage artifacts contain only paths/counts and no storage values or Document contents; the Bun 1.4 root gate passes.
>
> **Out of scope:** backend coverage (ticket 09), new product tests solely to raise the number, and browser E2E (ticket 14).

> *This was generated by AI during implementation.*
>
> Completed with `@vitest/coverage-v8`, a coverage-free fast `test`, and a required `test:coverage` lane included in root `ci`. Explicit `src/**/*.js`/`src/**/*.jsx` inclusion keeps unimported production files in the denominator. The 250-test complete run measured 85.71% lines, 87.61% functions, and 76.63% branches; reviewed floors are 85.70%/87.60%/76.60% and never auto-update. LCOV contains every discovered production module, while Markdown explicitly reports zero absent modules and identifies `src/main.jsx` as the one module with zero covered lines. A separate 100% probe produced exit code 1 from both Vitest thresholds and the postprocessor, proving the real gate fails regression without touching the checked baseline. CI regenerates/uploads LCOV, JSON summary, and path/count-only Markdown with `if: always()`. The Bun 1.4 root gate passed 178 backend + 250 frontend tests (428 total), coverage, typecheck, lint, build, workflow YAML parsing, and `git diff --check`.
