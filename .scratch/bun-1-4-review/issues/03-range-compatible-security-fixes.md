# 03 — Apply range-compatible dependency security fixes

**What to build:** Remove the dependency advisories that Bun can repair within the project's existing declared version ranges, prioritizing safe handling of hostile Documents while leaving major-version decisions for dedicated tickets.

**Blocked by:** 02 — Pin and qualify Bun 1.4.0.

**Category:** enhancement

**Status:** completed

- [x] The installed PDF parser is at or above the audited version that fixes arbitrary JavaScript execution from malicious PDFs.
- [x] Every range-compatible repair proposed by Bun is either applied or documented with a concrete compatibility reason for deferral.
- [x] Valid PDFs continue to create and complete Extraction jobs, while malformed or hostile PDF fixtures fail safely without script execution, process failure, or residual Source files.
- [x] The dependency and lockfile diff contains no unrelated major upgrades and is reviewed with Bun's package-diff evidence for sensitive parsing and networking packages.
- [x] The complete command surface passes after the updates.
- [x] A fresh audit clearly separates any remaining Vite/esbuild and ExcelJS/UUID decisions from the resolved in-range findings.

## Comments

> *This was generated by AI during triage.*
>
> ## Agent Brief
>
> **Category:** enhancement  
> **Summary:** Apply every repair Bun 1.4 can make inside the repository's declared dependency ranges, with the PDF parser vulnerability treated as the product-facing priority.
>
> **Current behavior:** `bun audit fix --dry-run` identifies 12 repairable findings in `brace-expansion`, `nanoid`, `pdfjs-dist`, `postcss`, and `undici`. The installed `pdfjs-dist@6.1.200` is below the `6.2.108` arbitrary-JavaScript fix. Five findings remain blocked by the direct Vite and ExcelJS dependency ranges and belong to tickets 04 and 05.
>
> **Desired behavior:** The installed graph contains the five compatible fixes without unrelated declared-major changes. The fixed PDF.js release and a behavior regression ensure valid, malformed, and script-bearing PDFs remain safely handled. A fresh audit reports only the separately owned Vite/esbuild and ExcelJS/UUID paths.
>
> **Key interfaces:** the Bun lockfile/installed dependency graph; `renderPdfPagesToPng`; PDF submission validation and Source-file cleanup; `bun audit fix --dry-run`; and `bun pm diff` review evidence.
>
> **Acceptance criteria:**
>
> - [x] A regression test requires `pdfjs-dist >=6.2.108`, whose current API no longer exposes the former dynamic-eval switch, and a script-bearing PDF does not execute its payload.
> - [x] Bun applies the five in-range package updates and a second dry run proposes no further range-compatible fixes.
> - [x] Valid, malformed, and script-bearing PDF regressions pass without observable script execution or retained temporary Source data.
> - [x] Audit/package-diff evidence shows only the Vite/esbuild and ExcelJS/UUID decisions remain.
> - [x] The complete root command surface passes under Bun 1.4.0.
>
> **Out of scope:** changing the direct Vite or ExcelJS major/range; adding the permanent CI package-hygiene policy from ticket 20; unrelated application dependency upgrades.

> *This was generated by AI during triage.*
>
> Completed. Bun upgraded only the five in-range packages it proposed: `brace-expansion@5.0.9`, `nanoid@3.3.18`, `pdfjs-dist@6.2.108`, `postcss@8.5.23`, and `undici@7.29.0`. The parser floor and script-bearing-PDF behavior are regression-tested; the existing valid JBIG2, generated valid PDF, malformed submission, process-survival, and Source-file cleanup paths remain green. `bun pm diff` was reviewed for PDF.js and Undici, `bun audit fix --dry-run` now fixes 0 findings, and the five residual advisories are exactly the Vite/esbuild and ExcelJS/UUID paths owned by tickets 04 and 05. A frozen install was unchanged and the full Bun 1.4 gate passed: typecheck, lint, 404 tests, and production build.
