# Verify No User-Visible Flow Depends On An Imaginary Route

Status: completed
Category: refactor
Type: AFK
User stories covered: 54, 56, 58-62

## Parent

`.scratch/workspace-hard-erasure-http-interface/PRD.md`

## What to build

Complete the PRD with an end-to-end HTTP Interface audit and local-runtime verification. Every user-visible frontend intent in scope must either use a backend behavior exercised through the real Seam or use the selected Better Auth Interface. Remove remaining raw mocks and unsupported route assumptions, keep React tests focused on presentation outcomes, and extend the local smoke path across the completed Workspace, profile, membership, invitation, Leave Workspace, Document deletion, search, and pagination flows.

This slice is verification and cleanup, not a new route registry or generated client.

## Acceptance criteria

- [ ] No user-visible frontend flow calls a `/v1` route or method that the backend does not implement.
- [ ] Profile behavior uses Better Auth session/update-user behavior and has no custom `/v1/profile` dependency.
- [ ] Workspace member listing/actions, Workspace invitation management, Leave Workspace, Document deletion, job search, and job pagination each have contract-oriented coverage through the real HTTP Seam.
- [ ] Workspace deletion smoke coverage proves physical hard-erasure and absence of database recreation after late work.
- [ ] Negative tests fail clearly if a formerly imaginary route is reintroduced into frontend domain request Modules.
- [ ] React tests mock domain intents/outcomes rather than acting as a second implementation of backend route responses.
- [ ] Domain request Modules own route construction and response/error normalization for the touched Workspace and Document flows.
- [ ] The local runtime smoke path verifies representative success and authorization failures across the completed flows.
- [ ] Backend and frontend domain terminology matches the current context glossaries, including Workspace deletion.
- [ ] No generic router framework, shared route-constant registry, generated client, or unrelated architecture refactor is introduced.
- [ ] `bun run typecheck`, `bun run test`, and `bun run build` all pass from the repository root.

## Blocked by

- `.scratch/workspace-hard-erasure-http-interface/issues/05-resume-interrupted-workspace-hard-erasure.md`
- `.scratch/workspace-hard-erasure-http-interface/issues/06-invalidate-session-browsers-after-workspace-deletion.md`
- `.scratch/workspace-hard-erasure-http-interface/issues/07-use-better-auth-for-profile.md`
- `.scratch/workspace-hard-erasure-http-interface/issues/09-manage-workspace-member-access.md`
- `.scratch/workspace-hard-erasure-http-interface/issues/10-manage-actionable-pending-workspace-invitations.md`
- `.scratch/workspace-hard-erasure-http-interface/issues/12-create-replacement-workspace-after-final-leave.md`
- `.scratch/workspace-hard-erasure-http-interface/issues/14-delete-live-documents-without-late-writes.md`
- `.scratch/workspace-hard-erasure-http-interface/issues/16-page-extraction-jobs-with-stable-cursors.md`

## Comments

> *This was generated by AI from the approved tracer-bullet breakdown.*

> *This was generated by AI during triage.*

## Agent Brief

**Category:** refactor
**Summary:** Verify every in-scope frontend intent is backed by a real server interface, consolidate remaining Workspace/Document request construction in their adapters, and protect the boundary with audit coverage.

**Current behavior:**
The completed routes now exist, but some Workspace and Document controller actions still hand-build endpoint paths. Existing browser tests often mock raw fetch responses, so they can conceal an unsupported route from a future regression.

**Desired behavior:**
Workspace and Document request adapters own their in-scope route construction and success normalization. Controller tests work in terms of those domain requests/outcomes. A contract audit exercises the supported operation matrix through the local Fetch app and rejects known former imaginary assumptions such as `/v1/profile`.

**Key interfaces:**
- Workspace and Document request adapters — the only in-scope controller boundary for HTTP path/method construction.
- Local Fetch application contract audit — confirms every adapter operation maps to an implemented route/method and rejects prohibited legacy dependencies.
- Local runtime smoke path — retains hard-erasure/late-work proof and samples the completed user-visible seam.

**Acceptance criteria:**
- [ ] In-scope Workspace/Document controller intents use their adapters; profile remains Better Auth-only with no `/v1/profile` dependency.
- [ ] Adapter contract/audit coverage fails for unsupported or former imaginary route assumptions and covers the completed HTTP operation matrix.
- [ ] Existing hard-erasure/late-work smoke evidence and previous focused contracts remain green.
- [ ] Domain terminology remains aligned and no generic route registry/client framework is introduced.
- [ ] Root typecheck, tests, lint, and build pass.

**Out of scope:**
- A generic router framework, generated client, global route constants, or a broad Template-controller redesign.
- New product behavior beyond verification and safely scoped adapter consolidation.

## Completion

Completed the HTTP interface audit and added adapter-boundary coverage for all in-scope Workspace/Document operations. The profile route guard rejects reintroducing the former custom profile dependency, Document controller route construction now resides in its adapter, and the real local runtime smoke path remains green. Full backend/frontend tests, lint, backend typecheck, and frontend production build passed.
