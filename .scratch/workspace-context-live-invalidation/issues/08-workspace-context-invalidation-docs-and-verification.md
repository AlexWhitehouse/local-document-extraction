# Workspace Context Invalidation Docs And Verification

Category: enhancement
Status: completed

## Parent

.scratch/workspace-context-live-invalidation/PRD.md

## What to build

Update the domain documentation to reflect the expanded live update and Workspace context invalidation language, then run the final focused verification pass across the completed slices. The docs should either expand **Workspace live update** to include Workspace context invalidation hints or introduce a separate documented term for the new event family.

The final verification should prove job lifecycle live updates remain responsive, live invalidations refresh selected Workspace context through bounded throttling, full Workspace-list refresh remains available for startup and access recovery, and sensitive billing or account details do not travel over websocket payloads.

## Acceptance criteria

- [x] Backend and frontend domain docs describe the implemented live update and Workspace context invalidation language.
- [x] If **Workspace live update** remains narrowly scoped to Workspace product data, a separate term such as **Workspace context invalidation** is documented.
- [x] Domain docs continue to state that Workspace live updates are not durable history and clients revalidate over HTTP after reconnecting.
- [x] Final verification confirms **Extraction job** lifecycle live updates update Documents UI without triggering Workspace context refresh.
- [x] Final verification confirms Workspace context invalidation refreshes selected Workspace context through bounded throttling.
- [x] Final verification confirms full Workspace-list refresh still works for startup, Stored workspace preference recovery, Workspace switching, pending Workspace invitation resolution, and access recovery.
- [x] Final verification confirms live update payloads exclude computed billing details, Stripe details, account identity, API keys, extracted answers, evidence text, Source file binary contents, and Document contents.
- [x] Backend typecheck passes.
- [x] Frontend production build passes.

## Blocked by

- .scratch/workspace-context-live-invalidation/issues/01-selected-workspace-context-refresh.md
- .scratch/workspace-context-live-invalidation/issues/02-workspace-live-update-invalidation-protocol.md
- .scratch/workspace-context-live-invalidation/issues/03-template-limit-invalidation.md
- .scratch/workspace-context-live-invalidation/issues/04-document-billing-usage-invalidation.md
- .scratch/workspace-context-live-invalidation/issues/05-workspace-access-and-member-limit-invalidation.md
- .scratch/workspace-context-live-invalidation/issues/06-workspace-billing-mutation-invalidation.md
- .scratch/workspace-context-live-invalidation/issues/07-stripe-billing-event-invalidation.md

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Document Workspace context invalidation language and run final verification for the completed live invalidation slices.

**Current behavior:**
The Workspace context live invalidation implementation slices are marked complete, and the codebase contains support for selected accepted **Workspace context** refresh, `workspace_context_invalidated` live update events, bounded frontend refresh scheduling, access revalidation, and payload-boundary tests. The remaining gap is the final closeout slice: domain documentation still needs to match the expanded live update language, and the completed behavior needs a focused final verification pass.

**Desired behavior:**
Backend and frontend domain documentation should describe the implemented relationship between **Workspace live update** events and **Workspace context invalidation**. If **Workspace live update** remains defined narrowly as realtime notification about changed **Workspace product data**, then **Workspace context invalidation** should be documented as a separate freshness-hint event family carried by the live update transport.

The final verification should confirm that **Extraction job** lifecycle live updates still update Documents UI state without causing Workspace context refresh, that Workspace context invalidations refresh the selected accepted **Workspace context** through bounded throttling, that full Workspace-list refresh remains available for startup and access-recovery flows, and that websocket payloads do not expose sensitive billing, identity, API key, extracted answer, evidence, Source file, or Document content.

**Key interfaces:**
- Backend domain language - should define the implemented live update and Workspace context invalidation terms using existing **Workspace**, **Workspace product data**, **Workspace context**, **Billing operational status**, **Extraction job**, **Source file**, and **Document** vocabulary.
- Frontend domain language - should describe how the browser treats live invalidation as a prompt to revalidate accepted **Workspace context** over HTTP, while keeping **Billing operational status** advisory and backend-owned.
- Workspace live update envelope - should continue to use the versioned batch message shape, including **Extraction job** lifecycle events and `workspace_context_invalidated` events with reason and occurrence timestamp only.
- Selected accepted **Workspace context** refresh - should remain the common live invalidation refresh path and should merge the returned Workspace entry without fetching pending **Workspace invitations** or unrelated Workspaces.
- Full Workspace-list refresh - should remain available for startup, Stored workspace preference recovery, Workspace switching, pending **Workspace invitation** resolution, and access recovery.

**Acceptance criteria:**
- [x] Backend and frontend domain docs describe the implemented live update and Workspace context invalidation language.
- [x] If **Workspace live update** remains narrowly scoped to Workspace product data, a separate term such as **Workspace context invalidation** is documented.
- [x] Domain docs continue to state that Workspace live updates are not durable history and clients revalidate over HTTP after reconnecting.
- [x] Final verification confirms **Extraction job** lifecycle live updates update Documents UI without triggering Workspace context refresh.
- [x] Final verification confirms Workspace context invalidation refreshes selected Workspace context through bounded throttling.
- [x] Final verification confirms full Workspace-list refresh still works for startup, Stored workspace preference recovery, Workspace switching, pending Workspace invitation resolution, and access recovery.
- [x] Final verification confirms live update payloads exclude computed billing details, Stripe details, account identity, API keys, extracted answers, evidence text, Source file binary contents, and Document contents.
- [x] Backend typecheck passes.
- [x] Frontend production build passes.

**Out of scope:**
- Changing Workspace billing product rules, Plan limits, Credit pricing, or backend enforcement for Document submission, Template limits, member limits, or the Billing ledger.
- Sending computed **Billing operational status**, remaining Credits, Plan page capacity, Stripe details, invoices, payment details, account identity, API keys, extracted answers, evidence text, Source file binary contents, or Document contents over Workspace live updates.
- Replacing HTTP Workspace context refresh with websocket-sourced state or making Workspace live updates durable history.
- Introducing new user-facing notifications for every invalidation.
- Reworking the Product Store Durable Object, Billing ledger Durable Object, or the selected Workspace context refresh contract beyond issues discovered during final verification.

## Verification Notes

- Added a focused domain documentation contract test that first failed because `**Workspace context invalidation**` was not documented, then passed after updating the backend and frontend domain docs.
- Backend docs now keep **Workspace live update** scoped to changed **Workspace product data** and document **Workspace context invalidation** as a separate freshness hint carried by the live update transport.
- Frontend docs now describe live invalidation as selected accepted **Workspace context** HTTP revalidation through bounded scheduling, with full Workspace-list refresh still reserved for startup, Stored workspace preference recovery, Workspace switching, pending **Workspace invitation** resolution, and access recovery.
- Live **Extraction job** lifecycle behavior, selected-refresh throttling, access revalidation, payload privacy, and selected/full refresh behavior were verified with the focused backend and frontend suites listed below.

## Verification Commands

- `npm test --prefix backend -- domainDocs`
- `npm test --prefix backend -- domainDocs index.live-updates workspaceProductStore api/workspaces`
- `npm test --prefix frontend -- useDocumentController.live-updates useWorkspaceController.selected-refresh App.billing`
- `npm test --prefix frontend -- App.workspace-toasts workspaceSelection App.auth`
- `npm run typecheck --prefix backend`
- `npm run build --prefix frontend`

## Completion Note

Issue 8 is complete. The final slice added domain-doc coverage, updated the backend and frontend domain language, and verified the completed Workspace context invalidation behavior without changing runtime code.
