# Verify Account Password Reset End To End

Status: completed

## Parent

.scratch/account-password-reset/PRD.md

## What to build

Perform the final verification pass for **Account password reset** across the full product behavior. Confirm the reset flow is usable end to end and that it only affects Account credentials, not **Workspace membership**, pending **Workspace invitations**, or **Workspace API keys**.

This slice should leave the feature ready to ship by running the focused automated checks, production build checks, and a documented manual flow check for the completed reset journey.

## Acceptance criteria

- [x] Focused backend tests for reset email rendering, Better Auth reset configuration, and reset-password policy guarding pass.
- [x] Focused frontend tests for reset-request mode, reset-link recovery states, and valid-token reset form pass.
- [x] Backend typecheck passes.
- [x] Frontend production build passes.
- [x] A manual or documented flow check verifies requesting a reset link, opening the reset experience, setting a valid password, and returning to sign in.
- [x] Verification confirms **Account password reset** does not create, remove, or alter **Workspace membership**.
- [x] Verification confirms **Account password reset** does not affect pending **Workspace invitations**.
- [x] Verification confirms **Account password reset** does not rotate or invalidate **Workspace API keys**.
- [x] Any residual test gaps or manual verification limitations are noted in the issue comments before marking complete.

## Blocked by

- .scratch/account-password-reset/issues/01-request-account-password-reset-link.md
- .scratch/account-password-reset/issues/02-show-reset-link-recovery-states.md
- .scratch/account-password-reset/issues/03-set-new-account-password-from-valid-reset-link.md

## Comments

> *This was generated by AI during final verification.*

## Verification Notes

- `npm run test --prefix backend -- src/lib/email/accountPasswordReset.test.ts src/lib/betterAuth.test.ts src/index.auth.test.ts` passed: 3 files, 21 tests.
- `npm run test --prefix frontend -- src/App.auth.test.jsx` passed: 1 file, 30 tests.
- `npm run typecheck --prefix backend` passed.
- `npm run build --prefix frontend` passed. Vite reported the existing Sonner `"use client"` bundle warning, but the build completed successfully.
- Documented flow check: frontend coverage verifies opening reset-request mode from sign-in with the typed email preserved, submitting the neutral reset-link request with `redirectTo: "/reset-password"`, rendering missing-token and token-error recovery states, rendering `/reset-password?token=...` as a new-password form, submitting `resetPassword` with the token and new password, clearing password fields, returning to sign in, and restoring `/`.
- Backend coverage verifies the reset email template contract, one-hour Better Auth reset token expiry, reset-session revocation, scheduled email sending through the existing transactional email path, weak reset-password rejection before Better Auth handling, and strong reset-password pass-through.
- Workspace boundary verification: the reset implementation uses Better Auth's built-in password-reset request and reset-password handling plus the existing account password-policy guard. It does not add product `/v1` routes, Workspace policy calls, Workspace membership writes, Workspace invitation writes, or Workspace API-key rotation/invalidation logic. Existing focused auth route coverage also preserves Workspace API-key behavior on product routes separately from session-only auth routes.
- Manual limitation: I did not perform a real inbox delivery and credential mutation against local Wrangler state because that requires valid local auth/email secrets and a mailbox check. The covered documented journey verifies the public UI and Worker integration points used by that manual flow.
