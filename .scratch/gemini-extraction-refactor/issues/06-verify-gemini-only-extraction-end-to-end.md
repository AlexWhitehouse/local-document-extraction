# Verify Gemini-Only Extraction End-to-End

Status: completed

Category: enhancement

## Parent

.scratch/gemini-extraction-refactor/PRD.md

## What to build

Verify the completed Gemini-only extraction refactor through the backend test and typecheck path. Add any missing integration-level assertions needed to show the **Extraction job lifecycle** still receives normalized results, preserves confidence and evidence, and keeps the completed **Extraction job** response shape unchanged.

## Acceptance criteria

- [x] Backend tests pass after all preceding slices are complete.
- [x] Backend typecheck passes after all preceding slices are complete.
- [x] Workflow-level coverage confirms raw model field results are normalized before completion.
- [x] Coverage confirms completed **Extraction job** result shape remains unchanged where existing tests or focused additions can verify it.
- [x] Coverage confirms confidence and evidence remain preserved through normalization and persistence where applicable.
- [x] No frontend changes are required for the refactor.

## Blocked by

- .scratch/gemini-extraction-refactor/issues/01-extract-durable-model-result-normalization.md
- .scratch/gemini-extraction-refactor/issues/02-make-extraction-model-configuration-explicit.md
- .scratch/gemini-extraction-refactor/issues/03-simplify-gemini-source-submission.md
- .scratch/gemini-extraction-refactor/issues/04-remove-inactive-provider-response-paths.md
- .scratch/gemini-extraction-refactor/issues/05-prune-unsupported-provider-configuration.md

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Verify the Gemini-only extraction refactor end-to-end and add focused integration coverage for durable result behaviour.

**Current behavior:**
The Gemini-only extraction refactor has been split across earlier completed slices. Existing backend coverage checks model configuration, Gemini source submission, response parsing, result normalization, and parts of the **Extraction job lifecycle**, but the final verification issue needs to confirm the completed refactor still works as one backend path.

**Desired behavior:**
The backend verification path should show that raw model field results are normalized before **Extraction job** completion, that completed **Extraction job** responses keep their existing result shape, and that confidence and evidence remain preserved through normalization, persistence, and API output where applicable. The refactor should not require frontend changes.

**Key interfaces:**
- `normalizeModelResults()` — converts requested **Template fields** plus raw model field results into durable **Extraction result** values.
- Cloudflare Workflow processing flow — should invoke Gemini extraction, normalize raw model field results, persist **Extraction results**, mark the **Extraction job** completed, and clean up the **Source file** without changing lifecycle semantics.
- Completed **Extraction job** detail response — should continue returning the existing result fields, including `answer`, `confidence`, and `evidence` when present.
- Backend verification commands — backend tests and backend typecheck should pass after the refactor.

**Acceptance criteria:**
- [x] Backend tests pass after all preceding Gemini-only extraction refactor slices are complete.
- [x] Backend typecheck passes after all preceding Gemini-only extraction refactor slices are complete.
- [x] Workflow-level coverage confirms raw model field results are normalized before **Extraction job** completion.
- [x] Coverage confirms completed **Extraction job** result shape remains unchanged where existing tests or focused additions can verify it.
- [x] Coverage confirms confidence and evidence remain preserved through normalization and persistence where applicable.
- [x] No frontend changes are made for this verification slice.

**Out of scope:**
- Supporting OpenAI, Anthropic, or any other non-Gemini provider.
- Changing **Document** submission API behaviour.
- Changing **Extraction job lifecycle** states, retry semantics, or Source file cleanup semantics.
- Changing the completed **Extraction job** API response shape.
- Wiring or removing extraction options.
- Changing frontend behaviour.
