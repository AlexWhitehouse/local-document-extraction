# Rename document processing workflow

Status: completed
Category: enhancement
Type: AFK
User stories covered: 28, 29, 30, 31, 32, 33

## Parent

`.scratch/document-extraction-terminology/PRD.md`

## What to build

Rename the application-owned background workflow to `documentProcessingWorkflow` while preserving **Extraction job lifecycle** behaviour, AI gateway integration, and **Source file** cleanup. Keep standards-level MIME strings and required platform API vocabulary unchanged where external contracts require `image`.

## Acceptance criteria

- [x] Application-owned workflow file, exported symbol, and test references use `documentProcessingWorkflow` naming.
- [x] Workflow processing still claims, processes, completes, and fails **Extraction jobs** according to the existing lifecycle rules.
- [x] AI prompt text uses **Document** terminology where application-owned wording is involved.
- [x] Required platform vocabulary and standards MIME strings remain intact.
- [x] Workflow tests still verify processing and **Source file** cleanup behaviour.
- [x] Backend typecheck succeeds.

## Blocked by

- `.scratch/document-extraction-terminology/issues/04-move-source-file-storage-to-non-legacy-r2-naming.md`

## Comments

> *This was generated by AI during triage.*

## Completion Notes

**What changed:**

- Issue 5 is complete because the blocker issue 4 is completed and the codebase already uses `DocumentProcessingWorkflow`, `DocumentProcessingWorkflowParams`, `DOCUMENT_PROCESSING_WORKFLOW`, and `documentProcessingWorkflow` naming in the backend workflow path.
- Workflow processing still loads, claims, processes, completes, fails, and cleans up **Source files** through the **Extraction job lifecycle** and `SOURCE_FILES_BUCKET` boundary.
- Required external vocabulary remains intact where appropriate, including Cloudflare Workflow types and standards/platform `image` terms.

**Verification:**

- `npm run typecheck --prefix backend`
- `npm test --prefix backend -- documentProcessingWorkflow.test.ts`
