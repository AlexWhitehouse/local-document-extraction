# Final terminology verification and builds

Status: completed
Category: enhancement
Type: AFK
User stories covered: 45, 46, 50

## Parent

`.scratch/document-extraction-terminology/PRD.md`

## What to build

Perform final verification that application-owned terminology has moved to **Document Extraction**, **Document**, and **Source file** language while only allowed generated, platform, or standards references retain `image`. Run the focused backend and frontend verification commands and fix any missed authored references.

## Acceptance criteria

- [x] Focused searches show no remaining application-owned legacy `image` terminology outside approved generated/platform/standards references.
- [x] API examples and support docs consistently use `document` for submission.
- [x] Backend typecheck succeeds.
- [x] Frontend production build succeeds.
- [x] Any remaining `image` references are documented as standards-level MIME, generated output, or required platform vocabulary.

## Verification notes

- Remaining frontend code references to `image/*` are standards-level MIME checks used to decide browser preview support and accepted source formats.
- Remaining backend generated references are in `worker-configuration.d.ts` and Cloudflare platform vocabulary.
- Remaining backend migration references are historical schema migrations or forward migrations that rename legacy columns to Source file terminology.
- Remaining tests assert legacy `image_name` aliases are not emitted and legacy `image` submission fields are not accepted.
- Domain docs retain `image` only to document forbidden legacy terminology or allowed standards/platform vocabulary.
- Backend typecheck: `npm run typecheck --prefix backend` passed.
- Frontend production build: `npm run build --prefix frontend` passed.

## Blocked by

- `.scratch/document-extraction-terminology/issues/02-expose-source-file-metadata-in-extraction-job-responses.md`
- `.scratch/document-extraction-terminology/issues/03-rename-source-file-byte-limits-through-workspace-flows.md`
- `.scratch/document-extraction-terminology/issues/04-move-source-file-storage-to-non-legacy-r2-naming.md`
- `.scratch/document-extraction-terminology/issues/05-rename-document-processing-workflow.md`
- `.scratch/document-extraction-terminology/issues/06-update-document-extraction-product-language-across-ui-and-support-docs.md`

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement

**Summary:** Perform final terminology verification for the Document Extraction cleanup and fix any remaining authored legacy `image` references.

**Current behavior:**
The terminology cleanup has been split across completed prerequisite issues covering the `document` submission field, **Source file** response metadata, **Source file** byte limits, Cloudflare resource naming, workflow naming, and user-facing/support documentation language. The final verification pass has not yet been completed, so there may still be application-owned legacy `image` terminology in code, tests, examples, or authored support material.

Some remaining `image` references are expected and should not be removed when they are standards-level MIME strings, generated output, Cloudflare/platform API vocabulary, HTML/browser vocabulary, or other externally required terminology.

**Desired behavior:**
Application-owned terminology should consistently use **Document Extraction**, **Document**, **Source file**, and **Extraction job** language. Focused searches should identify any remaining `image` references, classify allowed externally owned references, and fix missed application-owned references without changing runtime behavior beyond terminology consistency.

The verification should preserve the resolved strict contract: Document submission uses the `document` multipart field, legacy `image` and generic `file` fields are not advertised or accepted, and **Extraction job** responses expose **Source file** names without legacy aliases.

**Key interfaces:**
- Document submission API contract — should require the `document` multipart field and continue rejecting legacy `image` and generic `file` fields.
- **Extraction job** API response contract — should expose **Source file** metadata names such as `source_name` and should not emit legacy aliases such as `image_name`.
- Frontend document queueing and history UI — should render **Document** and **Source file** language while preserving previews for image MIME types where technically required.
- Authored API examples and support docs — should use **Document Extraction** naming and submit sources through the `document` multipart field.
- Terminology verification searches — should distinguish application-owned legacy wording from allowed generated, platform, browser, and standards vocabulary.

**Acceptance criteria:**
- [ ] Focused searches show no remaining application-owned legacy `image` terminology outside approved generated, platform, browser, or standards references.
- [ ] API examples and support docs consistently use **Document Extraction** naming and `document` for submission examples.
- [ ] Any remaining `image` references are documented or clearly classifiable as standards-level MIME strings, generated output, required platform/API vocabulary, browser/HTML vocabulary, or otherwise externally owned terms.
- [ ] Backend typecheck succeeds.
- [ ] Frontend production build succeeds.

**Out of scope:**
- Changing supported Document source formats.
- Reintroducing compatibility aliases for legacy `image` or generic `file` request fields.
- Emitting legacy API response aliases.
- Rewriting historical migration files.
- Hand-editing generated Cloudflare platform types or built frontend assets.
- Removing standards-level MIME strings such as `image/png`.
- Changing Cloudflare platform API option keys, generated types, or browser/HTML vocabulary that require image terminology.
