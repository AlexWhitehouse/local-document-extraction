# Add Workspace policy behaviour test harness

Status: done
Category: enhancement
Type: AFK
User stories covered: 45, 46

## Parent

`.scratch/workspace-policy/PRD.md`

## What to build

Add a focused backend test harness for `Workspace policy` behaviour using durable D1 fixtures or the closest available local substitute. Tests should cross the `Workspace policy` Interface and avoid asserting private implementation details.

## Acceptance criteria

- [x] The backend has a documented way to run focused `Workspace policy` behaviour tests.
- [x] Tests exercise external behaviour through the `Workspace policy` Interface rather than private helpers.
- [x] Tests cover at least one owner/admin/member permission path.
- [x] Tests cover at least one durable mutation path using D1 fixtures or a local substitute.
- [x] The test approach does not introduce a generic D1 store port solely for mocking.
- [x] Backend typecheck succeeds.

## Blocked by

- `.scratch/workspace-policy/issues/01-establish-workspace-policy-seam.md`

## Comments

> *This was generated by AI during triage.*

## Completion Notes

Verified that the backend has a focused Vitest harness for `Workspace policy` behaviour. The tests call the exported policy interface, use a local D1 fixture substitute, cover owner/admin/member permission paths, cover durable mutations, and do not introduce a generic D1 store port solely for mocking.
