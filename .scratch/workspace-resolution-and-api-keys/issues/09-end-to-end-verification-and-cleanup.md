# End-to-End Verification And Cleanup

Category: enhancement
Status: completed

## Parent

.scratch/workspace-resolution-and-api-keys/PRD.md

## What to build

Perform the final integrated pass across Workspace resolution, session-only SPA requests, Workspace API key display, persistence cleanup, Completed document cache, and backend API contracts. Remove obsolete assumptions around synthetic Workspace IDs, persisted API keys, and legacy storage, then verify the system end to end.

## Acceptance criteria

- [x] Obsolete frontend assumptions around synthetic default Workspace IDs and persisted API keys are removed.
- [x] Backend API contracts for Workspaces, Workspace API key generation, session-only routes, and external API-key product routes are verified together.
- [x] Frontend flows for startup, Workspace switching, create/delete/rename, invitation accept/decline, access loss, API key generation/rotation, sign-out, and completed Document cache are verified together.
- [x] Relevant pure module tests and API behavior tests pass.
- [x] Backend typecheck succeeds.
- [x] Frontend production build succeeds.
- [x] Any remaining old Image Extraction local storage references related to workspace preference are removed or intentionally left only where out of scope.

## Blocked by

- .scratch/workspace-resolution-and-api-keys/issues/01-backend-workspace-api-key-existence.md
- .scratch/workspace-resolution-and-api-keys/issues/02-backend-accepted-workspace-invariant-repair.md
- .scratch/workspace-resolution-and-api-keys/issues/03-session-only-spa-workspace-requests.md
- .scratch/workspace-resolution-and-api-keys/issues/04-frontend-workspace-resolution-state.md
- .scratch/workspace-resolution-and-api-keys/issues/05-frontend-workspace-context-transitions.md
- .scratch/workspace-resolution-and-api-keys/issues/06-frontend-persistence-clean-break.md
- .scratch/workspace-resolution-and-api-keys/issues/07-workspace-api-key-display-flow.md
- .scratch/workspace-resolution-and-api-keys/issues/08-completed-document-cache.md

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Perform the final integrated verification and cleanup pass for Workspace resolution, session-only SPA requests, Workspace API key display, persistence cleanup, Completed document cache, and backend API contracts.

**Current behavior:**
The feature work has been split across backend Workspace policy/API changes, frontend Workspace resolution and transition logic, browser persistence cleanup, Workspace API key display, and Completed document cache behavior. The tracker now needs a final pass that verifies these pieces operate together and removes any remaining obsolete assumptions around synthetic Workspace IDs, persisted Workspace API key material, and legacy Image Extraction storage.

**Desired behavior:**
The app should behave consistently with the Workspace Resolution and API Key PRD end to end. The SPA should resolve accepted Workspace context from backend-confirmed Workspaces, use session authentication plus `x-workspace-id` for UI requests, never use Workspace API keys as browser-session credentials, avoid synthetic fallback Workspace IDs, persist only the narrow Stored workspace preference, expose Workspace API key generation/rotation as an owner/admin external-client credential flow, and keep the Completed document cache bounded and scoped to accepted Workspace context. Backend contracts should continue to preserve accepted Workspace invariants, expose non-secret `has_api_key` state, issue one-time Workspace API key material only on explicit generation/rotation, and keep Workspace API keys restricted to external product-route access.

**Key interfaces:**
- `backend/src/api/workspaces.ts` and `backend/src/lib/workspacePolicy.ts` for Workspace listing repair, `has_api_key` response contracts, Workspace creation, Replacement personal Workspace, and Workspace API key generation/rotation.
- `backend/src/index.ts` authentication routing for session-only routes versus external Workspace API key product routes.
- `frontend/src/App.jsx` for startup, Workspace switching, invitation accept/decline, create/delete/rename, access loss, sign-out, request authentication, visible Workspace API key material, and cache cleanup wiring.
- `frontend/src/lib/workspaceSelection.js` for accepted Workspace context resolution and transition decisions.
- `frontend/src/lib/completedDocumentCache.js` for Completed document cache persistence, retention, pruning, and clearing rules.
- Existing focused test files under `backend/src/**/*.test.ts` and `frontend/src/**/*.test.{js,jsx}` for regression coverage.

**Acceptance criteria:**
- [x] Obsolete frontend assumptions around synthetic default Workspace IDs and persisted Workspace API keys are removed or intentionally confined to non-access display/test fixtures.
- [x] Backend API contracts for Workspaces, Workspace API key generation, session-only routes, and external API-key product routes are verified together.
- [x] Frontend flows for startup, Workspace switching, create/delete/rename, invitation accept/decline, access loss, API key generation/rotation, sign-out, and Completed document cache are verified together.
- [x] Relevant pure module tests and API behavior tests pass.
- [x] Backend typecheck succeeds with `npm run typecheck --prefix backend`.
- [x] Frontend production build succeeds with `npm run build --prefix frontend`.
- [x] Any remaining old Image Extraction local storage references related to workspace preference are removed or explicitly documented as out of scope.

**Out of scope:**
- Changing Workspace API key access parity on workspace-scoped product routes.
- Adding scoped Workspace API key permissions.
- Supporting Workspace API key access to the SPA shell.
- Migrating data from the legacy Image Extraction local storage key.
- Persisting Active page, selected Document, selected upload Template, Templates, job history, Source files, preview URLs, `File` objects, or blob URLs as user preferences.
- Reworking UI design beyond cleanup needed to satisfy the existing PRD.

**Notes for the agent:**
- Treat `workspace_local_default` carefully. It should not be used as accepted Workspace access or sent in API requests; if any remaining usage is only a non-access empty display fallback, either remove it or explain why it is safe.
- Do not reintroduce persistence of Workspace API key material. One-time key material should remain ephemeral and clear on dismiss, accepted Workspace change, sign-out, refresh, or navigation away.
- Issue 08 is marked completed but still has unchecked acceptance boxes in the tracker. Before starting, confirm whether those boxes are stale tracker state or whether Completed document cache work still needs finishing.
